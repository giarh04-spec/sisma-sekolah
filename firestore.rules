rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write access on all collections for sandbox demo environment
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
